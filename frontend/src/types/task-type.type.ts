export type TaskTypeType = {
  id: string,
  title: string,
  assignee: string,
  deadline: string,
  projects: string,
  priority: string,
  status: string,
  description: string,
  children?: [];
}
